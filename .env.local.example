# Sage Persuasion Colosseum demo
#
# The current demo does not require API keys.
# Wallets, tx hashes, credits, NFA identity, judging, and settlement are local-only.

NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: keep production demo data outside the git checkout.
# SAGE_DB_PATH=/var/lib/sage/db.json
